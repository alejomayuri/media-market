export default function ProductDescription({ description }) {
    return (
        <section className="product-description">
            <h2>Descripción</h2>
            <p>{description}</p>
        </section>
    )
}